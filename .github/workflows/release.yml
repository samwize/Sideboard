name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/build-release.yml
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
